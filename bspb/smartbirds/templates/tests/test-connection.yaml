apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smartbirds-chart.fullname" . }}-test-connection"
  labels:
    {{- include "smartbirds-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smartbirds-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
